export function max(num: number, max: number) {
  if (num > max) {
    return max
  }

  return num
}

export function min(num: number, min: number) {
  if (num < min) {
    return min
  }

  return num
}
